<UserControl x:Class="MVVMFirma.Views.RaportHistoriiZamowienView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" Width="600" HorizontalAlignment="Left">
    <Grid Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="Green" Padding="10">
            <TextBlock Text="Historia zamówień klienta" 
                       Foreground="White" 
                       FontSize="20" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Od:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Grid.Column="1" SelectedDate="{Binding DataOd}" Margin="5"/>

                <TextBlock Grid.Column="3" Text="Do:" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Grid.Column="4" SelectedDate="{Binding DataDo}" Margin="5"/>
            </Grid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Wybierz klienta:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding KlienciItems}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding Path=IdKlienta,Mode=TwoWay}" Margin="5"/>
            </Grid>
        </StackPanel>

        <Button Grid.Row="2" Content="Pokaż zamówienia" Command="{Binding ObliczCommand}"
                HorizontalAlignment="Center" Margin="5" Padding="25,7"
                Background="Green" Foreground="White" FontWeight="Bold"/>

        <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="10">
            <DataGrid ItemsSource="{Binding Zamowienia}" AutoGenerateColumns="False" CanUserAddRows="False" 
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id zamówienia" Binding="{Binding IdZamowienia}" />
                    <DataGridTextColumn Header="Kwota zamówienia" Binding="{Binding KwotaZamowienia}" />
                    <DataGridTextColumn Header="Data złożenia zamówienia" Binding="{Binding DataZlozenia}" />
                    <DataGridTextColumn Header="Metoda płatności" Binding="{Binding MetodaPlatnosci}" />
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>
