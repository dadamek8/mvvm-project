<local:JedenViewBase x:Class="MVVMFirma.Views.NowyStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="Nazwa" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="0" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Nazwa, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="1" Content="Opis" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="1" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Opis, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
    </Grid>
</local:JedenViewBase>
