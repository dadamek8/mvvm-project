<local:JedenViewBase x:Class="MVVMFirma.Views.NowyAdresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Content="Nazwa" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="0" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Miejscowosc, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="1" Content="Opis" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="1" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Ulica, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="2" Content="Nr domu" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="2" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=NrDomu, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="3" Content="Nr lokalu" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="3" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=NrLokalu, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="4" Content="Kod pocztowy" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="4" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=KodPocztowy, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="5" Content="Poczta" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="5" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Poczta, UpdateSourceTrigger=PropertyChanged}" Height="30"/>
    </Grid>
</local:JedenViewBase>
