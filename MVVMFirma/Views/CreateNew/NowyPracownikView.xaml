<local:JedenViewBase x:Class="MVVMFirma.Views.NowyPracownikView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="1" Content="Imię" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="1" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Imie, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="2" Content="Nazwisko" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="2" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Nazwisko, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="3" Content="Stanowisko" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="3" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Stanowisko, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="4" Content="E-mail" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="4" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Email, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="5" Content="Telefon" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <TextBox Grid.Column="1" Grid.Row="5" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" Text="{Binding Path=Telefon, UpdateSourceTrigger=PropertyChanged}" Height="30"/>

        <Label Grid.Column="0" Grid.Row="6" Content="Adres" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10"/>
        <ComboBox Grid.Column="1" Grid.Row="6" Width="250" HorizontalAlignment="Left" Margin="10" Padding="5" ItemsSource="{Binding AdresyItems}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding Path=IdAdresu,Mode=TwoWay}"/>
    </Grid>
</local:JedenViewBase>
