<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszyscyKlienciView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" SelectedItem="{Binding Path=WybranyKlient, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdKlienta}"/>
                <DataGridTextColumn Header="Rodzaj klienta" Binding="{Binding RodzajKlienta}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Telefon}"/>
                <DataGridTextColumn Header="Miejscowość" Binding="{Binding AdresMiejscowosc}"/>
                <DataGridTextColumn Header="Ulica" Binding="{Binding AdresUlica}"/>
                <DataGridTextColumn Header="Nr domu" Binding="{Binding AdresNrDomu}"/>
                <DataGridTextColumn Header="Nr lokalu" Binding="{Binding AdresNrLokalu}"/>
                <DataGridTextColumn Header="Kod pocztowy" Binding="{Binding AdresKodPocztowy}"/>
                <DataGridTextColumn Header="Poczta" Binding="{Binding AdresPoczta}"/>
                <DataGridTextColumn Header="Data rejestracji" Binding="{Binding DataRejestracji}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
