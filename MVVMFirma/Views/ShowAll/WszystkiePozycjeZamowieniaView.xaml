<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkiePozycjeZamowieniaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdPozycjiZamowienia}"/>
                <DataGridTextColumn Header="Nr zamówienia" Binding="{Binding ZamowienieId}"/>
                <DataGridTextColumn Header="Promocja" Binding="{Binding PromocjaNazwa}"/>
                <DataGridTextColumn Header="Produkt" Binding="{Binding ProduktNazwa}"/>
                <DataGridTextColumn Header="Ilość" Binding="{Binding Ilosc}"/>
                <DataGridTextColumn Header="Cena całkowita" Binding="{Binding CenaCalkowita}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
