<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkiePamieciRAMView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdPamieciRAM}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding ProduktNazwa}"/>
                <DataGridTextColumn Header="Producent" Binding="{Binding Producent}"/>
                <DataGridTextColumn Header="Łączna pojemoność (GB)" Binding="{Binding LacznaPojemnosc}"/>
                <DataGridTextColumn Header="Typ pamięci" Binding="{Binding TypPamieci}"/>
                <DataGridTextColumn Header="Częstotliwość pracy (MHz)" Binding="{Binding CzestotliwoscPracy}"/>
                <DataGridTextColumn Header="LiczbaModulow" Binding="{Binding LiczbaModulow}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
