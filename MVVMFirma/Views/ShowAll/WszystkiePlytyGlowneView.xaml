<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkiePlytyGlowneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdPlytyGlownej}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding ProduktNazwa}"/>
                <DataGridTextColumn Header="Producent" Binding="{Binding Producent}"/>
                <DataGridTextColumn Header="Gniazdo procesora" Binding="{Binding GniazdoProcesora}"/>
                <DataGridTextColumn Header="Sloty pamięci" Binding="{Binding SlotyPamieci}"/>
                <DataGridTextColumn Header="Wi-Fi" Binding="{Binding WiFi}"/>
                <DataGridTextColumn Header="Bluetooth" Binding="{Binding Bluetooth}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
