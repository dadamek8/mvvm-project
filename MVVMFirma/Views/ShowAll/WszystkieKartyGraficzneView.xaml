<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieKartyGraficzneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdKartyGraficznej}"/>
                <DataGridTextColumn Header="Nazwa" Binding="{Binding ProduktNazwa}"/>
                <DataGridTextColumn Header="Producent karty" Binding="{Binding ProducentKarty}"/>
                <DataGridTextColumn Header="Producent chipsetu" Binding="{Binding ProducentChipsetu}"/>
                <DataGridTextColumn Header="Chipset" Binding="{Binding Chipset}"/>
                <DataGridTextColumn Header="RAM (GB)" Binding="{Binding RAM}"/>
                <DataGridTextColumn Header="Taktowanie Rdzenia (MHz)" Binding="{Binding TaktowanieRdzenia}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
