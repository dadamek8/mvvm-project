<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieZamowieniaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}" SelectedItem="{Binding Path=WybraneZamowienie, Mode=TwoWay}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding IdZamowienia}"/>
                <DataGridTextColumn Header="E-mail klienta" Binding="{Binding KlientEmail}"/>
                <DataGridTextColumn Header="Metoda płatności" Binding="{Binding MetodaPlatnosciNazwa}"/>
                <DataGridTextColumn Header="Data złożenia" Binding="{Binding DataZlozenia}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding StatusNazwa}"/>
                <DataGridTextColumn Header="Kwota zamówienia" Binding="{Binding KwotaZamowienia}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:WszystkieViewBase>
