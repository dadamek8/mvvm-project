<local:WszystkieViewBase x:Class="MVVMFirma.Views.WszystkieDyskiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Id" Binding="{Binding IdDysku}"/>
                    <DataGridTextColumn Header="Nazwa" Binding="{Binding ProduktNazwa}"/>
                    <DataGridTextColumn Header="Producent" Binding="{Binding Producent}"/>
                    <DataGridTextColumn Header="Rodzaj" Binding="{Binding Rodzaj}"/>
                    <DataGridTextColumn Header="Format" Binding="{Binding Format}"/>
                    <DataGridTextColumn Header="Pojemność (GB)" Binding="{Binding Pojemnosc}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</local:WszystkieViewBase>
