<UserControl x:Class="MVVMFirma.Views.RaportSprzedanychSztukView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" Width="400" HorizontalAlignment="Left">
    <Grid Background="White">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="Blue" Padding="10">
            <TextBlock Text="Ilość sprzedanych produktów" 
               Foreground="White" 
               FontSize="20" 
               FontWeight="Bold" 
               HorizontalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Od:" VerticalAlignment="Center" Margin="5"/>
            <DatePicker Grid.Column="1" SelectedDate="{Binding DataOd}" Margin="5"/>

            <TextBlock Grid.Column="3" Text="Do:" VerticalAlignment="Center" Margin="5"/>
            <DatePicker Grid.Column="4" SelectedDate="{Binding DataDo}" Margin="5"/>

        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Wybierz produkt:" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10"/>
            <ComboBox Grid.Column="1" Grid.Row="2" Margin="10" Padding="5" ItemsSource="{Binding ProduktyItems}" DisplayMemberPath="Value" SelectedValuePath="Key" SelectedValue="{Binding Path=IdProduktu,Mode=TwoWay}"/>

        </Grid>

        <Button Grid.Row="3" Content="Oblicz" Command="{Binding ObliczCommand}"
         HorizontalAlignment="Center" Margin="5" Padding="25,7"
         Background="Blue" Foreground="White" FontWeight="Bold"/>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Grid.Row="0" Content="Sprzedane sztuki w tym okresie:" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10"/>
            <Label Grid.Column="1" Grid.Row="0" Content="{Binding SprzedaneSztuki}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10"/>

        </Grid>
        
    </Grid>
</UserControl>
